/**
 * 注册要求
 */
/*$('.submit').click(function(event){
                var name = $('.username').val();
				var pwd1 = $('.pass1').val();
				var pwd2 = $('.pass2').val();
				var checkb = $('.regxieyi').val();
				
                var namelen = name.length;
				var pwdlen = pwd1.length;
                if(namelen < 3 && namelen > 10){
                        alert("用户名长度在3-10之间，请重新输入！")
					    return false;
                        }
				
				if(pwdlen < 6 && pwdlen > 14){
                        alert("你输入的密码不正确，请重新输入！")
					    return false;
                        }
				
				if(pwd1!==pwd2||pwd1<1){
					alert("两次输入的密码不一样,请重新输入");
					$(".pass1,.pass2").val("");
					}
				
				if(checkb != true){
					return false;
				}
				
                });*/
/*document.getElementById("submit").onclick=aaa();
	function aaa() {
	var name =document.getElementById("username").value;
	var pwd1 = document.getElementById("pass1").value;
	var pwd2 = document.getElementById("pass2").value;
	var checkb = document.getElementById("regxieyi").value;
	
    var namelen = name.length;
	var pwdlen = pwd1.length;
    if(namelen < 3 && namelen > 10){
            alert("用户名长度在3-10之间，请重新输入！")
		    return false;
            }
	
	if(pwdlen < 6 && pwdlen > 14){
            alert("你输入的密码不正确，请重新输入！")
		    return false;
            }
	
	if(pwd1!==pwd2||pwd1<1){
		alert("两次输入的密码不一样,请重新输入");
		return false;
		}
	
	if(checkb != true){
		return false;
	}
}*/